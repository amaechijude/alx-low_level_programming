#include <stdio.h>

/*
 * main indicates the starting point of the code
 * return by default is 0 indicating success if there is no error
 * */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
