#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two input values
 * @a -is the first value
 * @b is the second value
 * Return a*b
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
