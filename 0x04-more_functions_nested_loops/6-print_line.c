#include "main.h"
/**
 * main -draws a shortest distance between two points
 * @n is the number of times
 * Returns 
 */
void print_line(int n)
{
	if (n <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int i;

		for (i = 1; i <= n; i++)
		{
			_putchar('_');
		}
		_putchar('\n');
	}
}
